<html>
    <body>
        <h1>Albums</h1>
        <% @albums.each do |album| %>
          <div><a href="/albums/<%= album.id %>">Title: <%= album.title %> Released: <%= album.release_year %></a></div>
        <% end %>
    </body>
</html>